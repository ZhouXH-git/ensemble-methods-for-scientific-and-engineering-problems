1. pretraining: related materials are under folder "/inputs/pretrain"

	run the bash script "run_pretrain":
	a. source the "init" file
		modify the address for your own "init" file under "utilities"
	b. pretrain the model with input file "input.yaml"
	c. get the pretraining result -- neural network's weight "w.1000"


2. run DAFI
	
	run the bash script "run_dafi":
	a. activate the DAFI environment
	b. activate OpenFOAM environment
	c. run DAFI
